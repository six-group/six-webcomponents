name: publish
on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'

env:
  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  PUPPETEER_SKIP_DOWNLOAD: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.ref }}
      - uses: actions/setup-node@v3
        with:
          node-version: 16
          registry-url: 'https://registry.npmjs.org'
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - run: npm install
      - run: lerna publish --yes --no-verify-access --registry https://registry.npmjs.org/:_authToken=${{secrets.NPM_PUBLISH_TOKEN}}

