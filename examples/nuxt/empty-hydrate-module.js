// Empty module to replace component-library/hydrate for client-side builds
// The hydrate module is only needed for SSR and contains Node.js dependencies
// that cannot be bundled for the browser
export default {};
